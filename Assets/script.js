// Dependencies
// Starting data
// Functions
// User interactions
// Initialize (i.e. run the start function)



var statBtn = document.getElementById("startButton");

function init() {
    //reset wins and losses count

    //start timer
}

startButton.addEventListener("click", init);